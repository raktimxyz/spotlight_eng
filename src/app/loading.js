import React from "react";

const loading = () => {
    return (
        <div className="loaderOverlay">
            <div className="loader"></div>
        </div>
    );
};

export default loading;
