export const metadata = {
    title: "Gallery Details Page",
    description: "Gallery Details Page Description",
};

export default function RootLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}